name: Build IRRemote IPA

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: IRRemote

      - name: Setup Xcode
        run: |
          ls /Applications | grep -i xcode
          XCODE_APP=$(ls -d /Applications/Xcode*.app 2>/dev/null | head -1)
          echo "Found Xcode: $XCODE_APP"
          if [ -n "$XCODE_APP" ]; then
            sudo xcode-select -s "$XCODE_APP/Contents/Developer"
          fi
          xcode-select -p
          xcrun --sdk iphoneos --show-sdk-version 2>/dev/null || echo "No iOS SDK found yet"

      - name: Install XcodeGen
        run: |
          HOMEBREW_NO_AUTO_UPDATE=1 brew install xcodegen 2>&1

      - name: Install iOS simulator runtime
        run: |
          xcrun simctl list > /dev/null
          xcodebuild -downloadPlatform iOS 2>&1 || echo "Download failed, trying alternative..."
          xcrun simctl runtime list 2>&1

      - name: Generate Xcode project
        run: |
          cd IRRemote
          xcodegen generate --spec project_V3.yml 2>&1
          ls -la *.xcodeproj

      - name: Build (unsigned)
        run: |
          cd IRRemote
          PROJ=$(ls -d *.xcodeproj 2>/dev/null | head -1)
          echo "Project: $PROJ"
          SDK=$(xcrun --sdk iphoneos --show-sdk-path 2>/dev/null)
          echo "SDK: $SDK"
          xcodebuild clean build \
            -project "$PROJ" \
            -scheme IRRemote \
            -configuration Release \
            -derivedDataPath build/derived \
            -destination generic/platform=iOS \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | tee build.log
          EXIT_CODE=${PIPESTATUS[0]}
          echo "=== xcodebuild EXIT CODE: $EXIT_CODE ==="
          echo "=== COMPILATION ERRORS ==="
          grep -nE ': error:|fatal error:|The following build commands failed' build.log | head -30 || echo "No errors found in grep"
          echo "=== FULL BUILD LOG ==="
          cat build.log
          echo "=== END BUILD LOG ==="
          if [ "$EXIT_CODE" -ne 0 ]; then exit $EXIT_CODE; fi

      - name: Show build errors in summary
        if: failure()
        run: |
          cd IRRemote
          echo "### Build failed - Last 100 lines" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          grep -iE 'error:|: error:|fatal|FAILED|The following build commands failed' build.log | head -50 >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "### Full log (last 100 lines)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          tail -100 build.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Package IPA
        run: |
          cd IRRemote
          APP_PATH=$(find build/derived -name "*.app" -type d | head -1)
          echo "App: $APP_PATH"
          if [ -z "$APP_PATH" ]; then echo "ERROR: No .app found!"; exit 1; fi
          mkdir -p build/ipa/Payload
          cp -r "$APP_PATH" build/ipa/Payload/
          cd build/ipa
          zip -r ../IRRemote.ipa Payload/
          echo "IPA size: $(wc -c < ../IRRemote.ipa) bytes"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: IRRemote.ipa
          path: IRRemote/build/IRRemote.ipa
          if-no-files-found: error
